org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=false

# ── Mod metadata ──────────────────────────────────────────────────────────────
mod_id=sojourner_bridge
mod_name=Sojourner Bridge
mod_version=0.1.0
mod_group=dev.sojourner.bridge
mod_authors=Sojourner
mod_license=MIT

# ── Platform versions ─────────────────────────────────────────────────────────
# PIN THESE to a real NeoForge build for your exact Minecraft version.
# Browse builds at https://projects.neoforged.net/neoforged/neoforge and grab the
# matching MDK; the placeholders below assume the repo's MC 26.2 target.
minecraft_version=26.2
minecraft_version_range=[26.2,26.3)
neo_version=26.2.0-beta.1
neo_version_range=[26.2,)
loader_version_range=[4,)

# ── Embedded libraries ────────────────────────────────────────────────────────
java_websocket_version=1.5.7
